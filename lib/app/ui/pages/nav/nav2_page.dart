import 'package:flutter/material.dart';

class Nav2Page extends StatelessWidget {

  const Nav2Page({ super.key });

   @override
   Widget build(BuildContext context) {
       return Scaffold(
           appBar: AppBar(title: const Text('Nav 02'),),
           body: Container(),
       );
  }
}