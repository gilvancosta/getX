import 'package:flutter/material.dart';

class Exemplo01Page extends StatelessWidget {
  const Exemplo01Page({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Exemplo 01'),
      ),
      body: Container(),
    );
  }
}
